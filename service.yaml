# Exposes the app so you can reach it in the browser.
apiVersion: v1
kind: Service
metadata:
  name: node-gitops-service
  namespace: demo
spec:
  selector:
    app: node-gitops
  ports:
    - port: 8080        # port on your machine
      targetPort: 3000  # port inside the pod
  type: LoadBalancer
