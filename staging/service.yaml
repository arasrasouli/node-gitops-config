apiVersion: v1
kind: Service
metadata:
  name: node-gitops-service
  namespace: demo-staging
spec:
  selector:
    app: node-gitops
  ports:
    - port: 8080
      targetPort: 3000
  type: LoadBalancer
